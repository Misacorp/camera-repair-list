{
  "name": "camera-repair-list-lambda",
  "version": "1.0.0",
  "description": "Camera repair list back end functions",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Misacorp",
  "license": "MIT",
  "dependencies": {
    "google-spreadsheet": "^2.0.4"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-airbnb-base": "^12.1.0",
    "eslint-plugin-import": "^2.11.0"
  }
}
