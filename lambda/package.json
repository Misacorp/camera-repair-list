{
  "name": "cameramakers-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function to fetch camera repair shop data from Google Sheets.",
  "main": "index.js",
  "dependencies": {
    "google-spreadsheet": "^2.0.4"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r lambda node_modules index.js"
  },
  "author": "Misacorp",
  "license": "MIT"
}
